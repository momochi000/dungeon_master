version: '3'
services:
  #dungeon:
  #  build:
  #    context: .
  #  volumes:
  #    - ./dungeon_master:/app/
  #    - ./ladybug/data:/app/ladybug/data
  #  environment:
  #    - OPENAI_API_KEY=$OPENAI_API_KEY
  #    - DATABASE_PATH=/app/ladybug/data/dungeon-master.lbug
  #  ports:
  #    - "61799:61799"
  #  tty: true
  #  stdin_open: true
  #  command: "lein repl :start :host 0.0.0.0 :port 61799"

  graphdb:
    image: ghcr.io/ladybugdb/explorer:latest
    volumes:
      - ./ladybug/data:/database
    environment:
      - LBUG_FILE=dungeon-master.lbug
    ports:
      - "8000:8000"

  pi:
    build:
      context: .
      dockerfile: ./Containerfile.pi
    env_file:
      - .env
    volumes:
      - ./:/app/
      - ./.pi/:/root/.pi/
